<%- include('partials/header'); %>
<div>
<% if (applications.length > 0) { %>
	<table class="table">
		<thead class="thead-dark">
			<tr>
				<th>Program</th>
                <th>Student</th>
                <th>Status</th>
                <th>Action</th>
			</tr>
		</thead>
		<tbody>
		<% for (let i = 0; i < applications.length; i++) { %>
                <tr>
                    <td><a href="/view_program/<%= applications[i].program._id %>"><%= applications[i].program.title %></a></td>
                    <td><a href="/view/<%= applications[i].student._id %>"><%= applications[i].student.first_name %></a></td>
                    <td><%= applications[i].status %></td>
                    <td>
                        <a href="application/accept/<%= applications[i].id %>" class="btn btn-sm btn-success">Accept</a>
                        <a href="application/deny/<%= applications[i].id %>" class="btn btn-sm btn-danger">Deny</a>
                    </td>
                </tr>
		<% } %>
		</tbody>
	</table>
<% } else { %>
	<p class="text-center">No applications found.</p>
<% } %>
</div>
</div>
</body>
</html>